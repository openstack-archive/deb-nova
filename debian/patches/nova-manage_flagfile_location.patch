--- a/bin/nova-manage
+++ b/bin/nova-manage
@@ -2307,7 +2307,11 @@
 
 def main():
     """Parse options and call the appropriate class/method."""
-    utils.default_flagfile()
+    cfg = "/etc/nova/nova.conf"
+    if not (os.path.exists(cfg) and os.access(cfg, os.R_OK)):
+	cfg = "nova.conf"
+    utils.default_flagfile(cfg)
+
     argv = FLAGS(sys.argv)
     logging.setup()
 
